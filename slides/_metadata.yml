metadata-files:
  - ../classlib/classlib/quarto/slides/hickernell-slides.yml

format:
  revealjs:
    navigation-mode: grid
    controls: true

execute:
  warning: false   # suppress runtime noise in slides
  message: false   # keep audience-facing output clean
